import { describe, expect, it } from "vitest";

// Teste de fumaça — prova que o Vitest (projeto "unit") está funcionando.
// Substituído por testes reais a partir da E05.
describe("sanity", () => {
  it("o ambiente de teste soma", () => {
    expect(1 + 1).toBe(2);
  });
});
